---
name: hack-vscode-model-picker-hide-upgrade-to-copilot-pro
description: hack vscode
argument-hint: apply|restore
---

`cd` the dir of this SKILL.md, execute `node index.mjs apply` or `node index.mjs restore` according to user's choice (default: `apply`).
